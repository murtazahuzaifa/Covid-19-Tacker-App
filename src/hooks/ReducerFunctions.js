export const handleChangeCountry = (state, action) => {
    switch (action.type) {
        case "global":
            const url = "";
            console.log(action.payload);
            return state

        case "country":
            const url = "";
            console.log(action.payload);
            return state

        default:
            return state
    }
}